version: '3.8'

services:
  bot:
    build: .
    restart: always
    volumes:
      - data:/app/prisma/data
    env_file:
      - .env
    environment:
      - DATABASE_URL=file:./data/dev.db

volumes:
  data:
